# Leave unset for local dev — Vite proxies /api to http://localhost:4000 (see vite.config.ts).
# In production, set this to your deployed API's base URL, including the /api suffix, e.g.:
# VITE_API_URL=https://your-api.onrender.com/api
