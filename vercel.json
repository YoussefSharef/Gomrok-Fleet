{
  "installCommand": "npm ci",
  "buildCommand": "npm run prisma:deploy --workspace server && npm run seed --workspace server && npm run build --workspace client",
  "outputDirectory": "client/dist",
  "rewrites": [{ "source": "/((?!api/).*)", "destination": "/index.html" }]
}
