{
  "buildCommand": "npx prisma migrate deploy --schema server/prisma/schema.prisma && npx tsx server/prisma/seed.ts && npm run build --workspace client",
  "outputDirectory": "client/dist",
  "rewrites": [{ "source": "/((?!api/).*)", "destination": "/index.html" }]
}
